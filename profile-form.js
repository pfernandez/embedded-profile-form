jQuery(document).ready(function($) {

    // Show/hide the profile settings with a jQuery fade.
    /*
    if( typeof( $.fn.modal ) == 'undefined' ) {
        $(".profile-form").hide();
        $(".show-settings").click(function() {
            $(".profile-form").fadeToggle();
        });
    }
    */
});